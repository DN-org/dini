//Packages
import styled from "styled-components";

export const CatalogСontent = styled("div")(() => ({
    maxWidth: "1280px",
    margin: 'auto',
    backgroundColor: 'black',
    padding: '40px 0',   
}));